const mixins = Object.freeze({
  primaryGradient:
    'background: linear-gradient(90deg, #6417CB 0%, #74C6FA 100%);',
  textPrimaryGradient:
    'background: linear-gradient(90deg, #6417CB 0%, #74C6FA 100%); background: -webkit-gradient(linear, left middle, left middle, from(#6417cb), to(#74c6fa) ); background-clip: text; -webkit-text-fill-color: transparent; -webkit-background-clip: text;'
})

export { mixins }
