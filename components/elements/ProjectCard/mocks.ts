import { mockProject } from '@utils/tests/mocks/mockProject'

import { ProjectCardProps } from './ProjectCard'

export const mockProjectCardProps: ProjectCardProps = {
  alternateSide: false,
  ...mockProject
}
